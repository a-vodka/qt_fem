#include "FePrintWriter.h"
#include "UTIL.h"

namespace util
{

    /*PrintWriter *FePrintWriter::getPrinter(const std::wstring &fileOut)
	{
		try
		{
			BufferedWriter tempVar(new FileWriter(fileOut));
			PR = new PrintWriter(&tempVar);
		}
		catch (const std::runtime_error &e)
		{
		UTIL::errorMsg(L"Cannot open output file: " + fileOut);
		}
		return PR;
	}
*/
}
